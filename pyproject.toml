[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "solar-uq"
version = "0.1.0"
requires-python = ">=3.12"
description = "Solar GHI forecasting with deep learning and uncertainty quantification"

[tool.setuptools.packages.find]
where = ["src"]
